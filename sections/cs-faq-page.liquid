{%- liquid
  assign section_id = section.id
  assign anchor_id = 'csf-' | append: section_id
-%}

<section
  id="{{ anchor_id }}"
  class="csf-section"
  data-csf-root
  data-csf-single-open="{{ section.settings.single_open }}"
  style="
    --csf-bg: {{ section.settings.bg_color }};
    --csf-overlay-color: {{ section.settings.overlay_color }};
    --csf-overlay-opacity: {{ section.settings.overlay_opacity | divided_by: 100.0 }};
    --csf-panel-bg: {{ section.settings.panel_bg_color }};
    --csf-panel-border: {{ section.settings.panel_border_color }};
    --csf-heading: {{ section.settings.heading_color }};
    --csf-item-bg: {{ section.settings.item_bg_color }};
    --csf-item-text: {{ section.settings.item_text_color }};
    --csf-item-active-text: {{ section.settings.item_active_text_color }};
    --csf-answer-text: {{ section.settings.answer_color }};
    --csf-pad-top: {{ section.settings.padding_top }}px;
    --csf-pad-bottom: {{ section.settings.padding_bottom }}px;
    --csf-max-width: {{ section.settings.content_max_width }}px;
    --csf-heading-size: {{ section.settings.heading_size }}px;
    --csf-panel-radius: {{ section.settings.panel_radius }}px;
    --csf-item-radius: {{ section.settings.item_radius }}px;
    {% if section.settings.bg_image != blank %}
      --csf-bg-image: url('{{ section.settings.bg_image | image_url: width: 2400 }}');
    {% else %}
      --csf-bg-image: url('{{ 'chocolate-fabric.png' | asset_url }}');
    {% endif %}
    {% if section.settings.panel_image != blank %}
      --csf-panel-image: url('{{ section.settings.panel_image | image_url: width: 1800 }}');
    {% else %}
      --csf-panel-image: url('{{ 'gold-fabric.png' | asset_url }}');
    {% endif %}
  "
>
  <div class="csf-panel" data-csf-reveal>
    {%- if section.settings.heading != blank -%}
      <h1 class="csf-heading">{{ section.settings.heading }}</h1>
    {%- endif -%}

    {%- if section.blocks.size > 0 -%}
      <ul class="csf-list" role="list">
        {%- for block in section.blocks -%}
          {%- if block.type == 'faq_item' -%}
            {%- assign item_id = anchor_id | append: '-item-' | append: block.id -%}
            {%- assign panel_id = item_id | append: '-panel' -%}
            <li class="csf-item" data-csf-reveal {{ block.shopify_attributes }}>
              <button
                type="button"
                class="csf-question"
                aria-expanded="false"
                aria-controls="{{ panel_id }}"
                id="{{ item_id }}"
                data-csf-toggle
              >
                <span class="csf-question__icon" aria-hidden="true"></span>
                <span class="csf-question__text">{{ block.settings.question }}</span>
              </button>
              <div
                class="csf-answer"
                id="{{ panel_id }}"
                role="region"
                aria-labelledby="{{ item_id }}"
                hidden
              >
                <div class="csf-answer__inner">
                  {{ block.settings.answer }}
                </div>
              </div>
            </li>
          {%- endif -%}
        {%- endfor -%}
      </ul>
    {%- endif -%}

    <div class="csf-footer-rule" aria-hidden="true"></div>
  </div>
</section>

{%- if section.blocks.size > 0 -%}
  <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "FAQPage",
      "mainEntity": [
        {%- for block in section.blocks -%}
          {%- if block.type == 'faq_item' -%}
            {
              "@type": "Question",
              "name": {{ block.settings.question | json }},
              "acceptedAnswer": {
                "@type": "Answer",
                "text": {{ block.settings.answer | strip_html | strip_newlines | json }}
              }
            }{%- unless forloop.last -%},{%- endunless -%}
          {%- endif -%}
        {%- endfor -%}
      ]
    }
  </script>
{%- endif -%}

{%- style -%}
  #shopify-section-{{ section.id }}.shopify-section,
  #shopify-section-{{ section.id }} .csf-section {
    background-color: var(--csf-bg, #3d2817);
    background-image: var(--csf-bg-image);
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    padding: var(--csf-pad-top, 80px) 24px var(--csf-pad-bottom, 80px);
    font-family: 'Barlow', 'Helvetica Neue', Arial, sans-serif;
    -webkit-font-smoothing: antialiased;
    position: relative;
    overflow: hidden;
  }

  #shopify-section-{{ section.id }} .csf-section::before {
    content: '' !important;
    position: absolute !important;
    inset: 0 !important;
    background-color: var(--csf-overlay-color, #000000) !important;
    opacity: var(--csf-overlay-opacity, 0) !important;
    pointer-events: none !important;
    z-index: 0 !important;
  }

  #shopify-section-{{ section.id }} .csf-panel {
    z-index: 1 !important;
    max-width: var(--csf-max-width, 820px) !important;
    margin: 0 auto !important;
    width: 100% !important;
    background-color: var(--csf-panel-bg, #9b6802) !important;
    background-image: var(--csf-panel-image) !important;
    background-size: cover !important;
    background-position: center !important;
    border: 2px solid var(--csf-panel-border, #e2ab43) !important;
    border-radius: var(--csf-panel-radius, 36px) !important;
    padding: 36px 36px 28px !important;
    box-shadow: 0 18px 48px rgba(0, 0, 0, 0.32) !important;
    position: relative !important;
    box-sizing: border-box !important;
  }

  #shopify-section-{{ section.id }} .csf-heading {
    font-family: 'OAXACA', Georgia, 'Times New Roman', serif !important;
    font-size: var(--csf-heading-size, 34px) !important;
    line-height: 1.1 !important;
    letter-spacing: 0.04em !important;
    color: var(--csf-heading, #3d2817) !important;
    margin: 0 0 24px !important;
    text-align: center !important;
    font-weight: 700 !important;
    text-transform: uppercase !important;
  }

  #shopify-section-{{ section.id }} .csf-list {
    list-style: none !important;
    margin: 0 !important;
    padding: 0 !important;
    display: flex !important;
    flex-direction: column !important;
    gap: 8px !important;
  }

  #shopify-section-{{ section.id }} .csf-item {
    background: var(--csf-item-bg, #5c402f) !important;
    border-radius: var(--csf-item-radius, 6px) !important;
    overflow: hidden !important;
    transition: box-shadow 280ms cubic-bezier(0.2, 0.8, 0.2, 1) !important;
    list-style: none !important;
    margin: 0 !important;
    padding: 0 !important;
    display: block !important;
    width: 100% !important;
  }

  #shopify-section-{{ section.id }} .csf-item:has(.csf-question[aria-expanded="true"]) {
    box-shadow: 0 0 0 2px var(--csf-panel-border, #e2ab43) !important;
  }

  #shopify-section-{{ section.id }} .csf-question {
    appearance: none !important;
    -webkit-appearance: none !important;
    background: transparent !important;
    background-color: transparent !important;
    background-image: none !important;
    border: 0 !important;
    border-radius: 0 !important;
    color: var(--csf-item-text, #e2ab43) !important;
    width: 100% !important;
    min-width: 0 !important;
    max-width: none !important;
    padding: 14px 20px !important;
    margin: 0 !important;
    display: flex !important;
    align-items: center !important;
    gap: 12px !important;
    cursor: pointer !important;
    font-family: 'OAXACA', Georgia, 'Times New Roman', serif !important;
    font-size: 14px !important;
    line-height: 1.3 !important;
    font-weight: 700 !important;
    letter-spacing: 0.08em !important;
    text-transform: uppercase !important;
    text-align: left !important;
    text-decoration: none !important;
    box-shadow: none !important;
    text-shadow: none !important;
    transition: color 220ms ease !important;
    box-sizing: border-box !important;
  }

  #shopify-section-{{ section.id }} .csf-question:hover,
  #shopify-section-{{ section.id }} .csf-question:focus,
  #shopify-section-{{ section.id }} .csf-question:active {
    background: transparent !important;
    background-color: transparent !important;
    text-decoration: none !important;
    box-shadow: none !important;
    color: var(--csf-item-text, #e2ab43) !important;
  }

  #shopify-section-{{ section.id }} .csf-question:focus-visible {
    outline: 2px solid var(--csf-panel-border, #e2ab43) !important;
    outline-offset: -2px !important;
  }

  #shopify-section-{{ section.id }} .csf-question[aria-expanded="true"],
  #shopify-section-{{ section.id }} .csf-question[aria-expanded="true"]:hover,
  #shopify-section-{{ section.id }} .csf-question[aria-expanded="true"]:focus {
    color: var(--csf-item-active-text, #f9f2ef) !important;
  }

  #shopify-section-{{ section.id }} .csf-question__icon {
    flex: 0 0 auto !important;
    width: 16px !important;
    height: 16px !important;
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    font-weight: 400 !important;
    line-height: 1 !important;
    font-family: 'Barlow', sans-serif !important;
    font-size: 0 !important;
    color: inherit !important;
    transition: transform 260ms cubic-bezier(0.2, 0.8, 0.2, 1) !important;
  }

  #shopify-section-{{ section.id }} .csf-question[aria-expanded="true"] .csf-question__icon::before {
    content: '\2212' !important;
    font-size: 18px !important;
    color: inherit !important;
  }

  #shopify-section-{{ section.id }} .csf-question[aria-expanded="false"] .csf-question__icon::before {
    content: '+' !important;
    font-size: 18px !important;
    color: inherit !important;
  }

  #shopify-section-{{ section.id }} .csf-question__text {
    flex: 1 1 auto !important;
    color: inherit !important;
  }

  #shopify-section-{{ section.id }} .csf-answer {
    display: grid !important;
    grid-template-rows: 0fr;
    transition: grid-template-rows 340ms cubic-bezier(0.2, 0.8, 0.2, 1);
  }

  #shopify-section-{{ section.id }} .csf-answer[data-csf-open="true"] {
    grid-template-rows: 1fr;
  }

  #shopify-section-{{ section.id }} .csf-answer[hidden] {
    display: none !important;
  }

  #shopify-section-{{ section.id }} .csf-answer__inner {
    overflow: hidden !important;
    min-height: 0 !important;
    padding: 0 20px;
  }

  #shopify-section-{{ section.id }} .csf-answer[data-csf-open="true"] .csf-answer__inner {
    padding: 2px 20px 18px !important;
  }

  #shopify-section-{{ section.id }} .csf-answer__inner,
  #shopify-section-{{ section.id }} .csf-answer__inner p {
    color: var(--csf-answer-text, #d4c5a9) !important;
    font-family: 'Barlow', 'Helvetica Neue', Arial, sans-serif !important;
    font-size: 13px !important;
    line-height: 1.6 !important;
    margin: 0 0 8px !important;
    font-weight: 500 !important;
  }

  #shopify-section-{{ section.id }} .csf-answer__inner p:last-child {
    margin-bottom: 0 !important;
  }

  #shopify-section-{{ section.id }} .csf-answer__inner ul,
  #shopify-section-{{ section.id }} .csf-answer__inner ol {
    margin: 0 0 8px !important;
    padding-left: 20px !important;
  }

  #shopify-section-{{ section.id }} .csf-answer__inner a {
    color: var(--csf-item-text, #e2ab43) !important;
    text-decoration: underline !important;
    text-underline-offset: 3px !important;
  }

  #shopify-section-{{ section.id }} .csf-answer__inner strong {
    font-weight: 700 !important;
    color: var(--csf-item-active-text, #f9f2ef) !important;
  }

  #shopify-section-{{ section.id }} .csf-footer-rule {
    margin: 22px auto 0 !important;
    width: 100% !important;
    height: 1px !important;
    background-image: linear-gradient(
      to right,
      transparent 0,
      var(--csf-heading, #3d2817) 50%,
      transparent 100%
    ) !important;
    background-size: 6px 1px !important;
    background-repeat: repeat-x !important;
    opacity: 0.35 !important;
    border: 0 !important;
  }

  #shopify-section-{{ section.id }} [data-csf-reveal] {
    opacity: 0;
    transform: translateY(14px);
    transition: opacity 520ms ease, transform 520ms cubic-bezier(0.2, 0.8, 0.2, 1);
    will-change: opacity, transform;
  }

  #shopify-section-{{ section.id }} [data-csf-reveal].csf-is-visible {
    opacity: 1;
    transform: none;
  }

  @media (prefers-reduced-motion: reduce) {
    #shopify-section-{{ section.id }} .csf-item,
    #shopify-section-{{ section.id }} .csf-question,
    #shopify-section-{{ section.id }} .csf-question__icon,
    #shopify-section-{{ section.id }} .csf-answer,
    #shopify-section-{{ section.id }} [data-csf-reveal] {
      transition: none !important;
    }
    #shopify-section-{{ section.id }} [data-csf-reveal] {
      opacity: 1;
      transform: none;
    }
  }

  @media (max-width: 640px) {
    #shopify-section-{{ section.id }} .csf-section {
      padding-left: 16px !important;
      padding-right: 16px !important;
    }
    #shopify-section-{{ section.id }} .csf-panel {
      padding: 24px 18px 20px !important;
      border-radius: calc(var(--csf-panel-radius, 36px) * 0.6) !important;
    }
    #shopify-section-{{ section.id }} .csf-heading {
      font-size: calc(var(--csf-heading-size, 34px) * 0.78) !important;
      margin-bottom: 18px !important;
    }
    #shopify-section-{{ section.id }} .csf-question {
      padding: 12px 14px !important;
      font-size: 12px !important;
      letter-spacing: 0.06em !important;
    }
    #shopify-section-{{ section.id }} .csf-answer[data-csf-open="true"] .csf-answer__inner {
      padding: 2px 14px 14px !important;
    }
  }
{%- endstyle -%}

{% javascript %}
  (function () {
    var reduceMotion = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    function initAccordion(root) {
      if (root.dataset.csfInit === '1') return;
      root.dataset.csfInit = '1';

      var singleOpen = root.getAttribute('data-csf-single-open') === 'true';
      var toggles = root.querySelectorAll('[data-csf-toggle]');

      toggles.forEach(function (btn) {
        btn.addEventListener('click', function () {
          var expanded = btn.getAttribute('aria-expanded') === 'true';
          var panel = document.getElementById(btn.getAttribute('aria-controls'));
          if (!panel) return;

          if (singleOpen && !expanded) {
            toggles.forEach(function (other) {
              if (other === btn) return;
              if (other.getAttribute('aria-expanded') === 'true') {
                closePanel(other);
              }
            });
          }

          if (expanded) {
            closePanel(btn);
          } else {
            openPanel(btn, panel);
          }
        });
      });
    }

    function openPanel(btn, panel) {
      panel.hidden = false;
      void panel.offsetHeight;
      panel.setAttribute('data-csf-open', 'true');
      btn.setAttribute('aria-expanded', 'true');
    }

    function closePanel(btn) {
      var panel = document.getElementById(btn.getAttribute('aria-controls'));
      if (!panel) return;
      btn.setAttribute('aria-expanded', 'false');
      panel.removeAttribute('data-csf-open');
      if (reduceMotion) {
        panel.hidden = true;
        return;
      }
      var onEnd = function (e) {
        if (e.propertyName !== 'grid-template-rows') return;
        panel.hidden = true;
        panel.removeEventListener('transitionend', onEnd);
      };
      panel.addEventListener('transitionend', onEnd);
    }

    function initReveal(root) {
      var targets = root.querySelectorAll('[data-csf-reveal]');
      if (!targets.length) return;

      if (reduceMotion || !('IntersectionObserver' in window)) {
        targets.forEach(function (el) { el.classList.add('csf-is-visible'); });
        return;
      }

      var io = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
          if (entry.isIntersecting) {
            entry.target.classList.add('csf-is-visible');
            io.unobserve(entry.target);
          }
        });
      }, { threshold: 0.12, rootMargin: '0px 0px -8% 0px' });

      targets.forEach(function (el, i) {
        el.style.transitionDelay = Math.min(i * 35, 320) + 'ms';
        io.observe(el);
      });
    }

    function boot() {
      document.querySelectorAll('[data-csf-root]').forEach(function (root) {
        initAccordion(root);
        initReveal(root);
      });
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', boot);
    } else {
      boot();
    }

    document.addEventListener('shopify:section:load', function (e) {
      var root = e.target.querySelector('[data-csf-root]');
      if (root) {
        initAccordion(root);
        initReveal(root);
      }
    });
  })();
{% endjavascript %}

{% schema %}
{
  "name": "FAQ",
  "tag": "section",
  "class": "cs-faq-section",
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Frequently Asked Questions"
    },
    {
      "type": "checkbox",
      "id": "single_open",
      "label": "Single open",
      "info": "Only one item open at a time.",
      "default": true
    },
    {
      "type": "header",
      "content": "Section background"
    },
    {
      "type": "image_picker",
      "id": "bg_image",
      "label": "Background image",
      "info": "Defaults to chocolate-fabric.png."
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background color",
      "default": "#3d2817"
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "Overlay color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Overlay opacity",
      "min": 0,
      "max": 100,
      "step": 5,
      "default": 40,
      "unit": "%"
    },
    {
      "type": "header",
      "content": "Panel"
    },
    {
      "type": "image_picker",
      "id": "panel_image",
      "label": "Panel image",
      "info": "Defaults to gold-fabric.png."
    },
    {
      "type": "color",
      "id": "panel_bg_color",
      "label": "Panel fallback",
      "default": "#9b6802"
    },
    {
      "type": "color",
      "id": "panel_border_color",
      "label": "Panel border",
      "default": "#e2ab43"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading color",
      "default": "#3d2817"
    },
    {
      "type": "range",
      "id": "panel_radius",
      "label": "Panel radius",
      "min": 0,
      "max": 60,
      "step": 2,
      "default": 36,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "Item"
    },
    {
      "type": "color",
      "id": "item_bg_color",
      "label": "Item background",
      "default": "#5c402f"
    },
    {
      "type": "color",
      "id": "item_text_color",
      "label": "Question (closed)",
      "default": "#e2ab43"
    },
    {
      "type": "color",
      "id": "item_active_text_color",
      "label": "Question (open)",
      "default": "#f9f2ef"
    },
    {
      "type": "color",
      "id": "answer_color",
      "label": "Answer text",
      "default": "#d4c5a9"
    },
    {
      "type": "range",
      "id": "item_radius",
      "label": "Item radius",
      "min": 0,
      "max": 20,
      "step": 2,
      "default": 6,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "content_max_width",
      "label": "Max width",
      "min": 600,
      "max": 1400,
      "step": 20,
      "default": 820,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "heading_size",
      "label": "Heading size",
      "min": 22,
      "max": 56,
      "step": 2,
      "default": 34,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top padding",
      "min": 0,
      "max": 200,
      "step": 10,
      "default": 80,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom padding",
      "min": 0,
      "max": 200,
      "step": 10,
      "default": 80,
      "unit": "px"
    }
  ],
  "blocks": [
    {
      "type": "faq_item",
      "name": "FAQ item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "default": "What is a serving size?"
        },
        {
          "type": "richtext",
          "id": "answer",
          "label": "Answer",
          "default": "<p>Share the answer here.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "FAQ",
      "blocks": [
        { "type": "faq_item" },
        { "type": "faq_item" },
        { "type": "faq_item" }
      ]
    }
  ]
}
{% endschema %}
